#!/usr/bin/env bash
# displayin 12 hours and 59 minutes

hour=0
while ((hour < 13))
do
    echo "Hour: $hour"

    minute=1
    while ((minute < 60))
    do
	echo "$minute"
	minute=$((minute+1))
    done
    hour=$((hour+1))
done
